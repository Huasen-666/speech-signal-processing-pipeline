# Old Experiment Outputs

This folder contains older generated experiment outputs. They are archived here so the active `experiments` folder stays focused on the current v2 workflow.

Current v2 outputs live at:

- `experiments/dave_bp_consecutive_dataset_v2`
- `experiments/dave_bp_drb_validation_dataset_v2`
- `experiments/per_patient_bp_eval/session_shift_fixes`

Do not use old outputs for new training unless you are intentionally reproducing an older result.
