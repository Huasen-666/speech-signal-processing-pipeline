# Data Directory

Keep private recordings out of Git.

Recommended local layout:

```text
data/
  raw/          # original recordings, ignored by git
  processed/    # cleaned audio and generated clips, ignored by git
```

For public examples, use anonymized, consent-safe, or synthetic audio only.

